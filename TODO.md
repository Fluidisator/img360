# TODO

- **Implementation (or embedding) of a real 3D sphere image viewer**
  - Integrate a 3D viewer to allow interactive visualization of 360° images mapped onto a sphere.
  - Evaluate existing open-source viewers (e.g., Photo Sphere Viewer, three.js, or OpenGL-based solutions).
  - Consider embedding a web-based viewer in the GUI or providing a standalone viewer utility.
  - Support for mouse/touch navigation and zoom.

- **Exif metadata**
  - Read and preserve Exif metadata during image processing.
  - Add or update Exif tags relevant to 360° images (e.g., projection type, orientation, camera info).
  - Optionally, provide a way to edit Exif metadata from the GUI.
